# Docker Compose Environment Variables
# Copy this file to .env and customize for your machine

# User ID and Group ID for file permissions
# On Linux/macOS, run: id -u and id -g to get your values
# On Windows with WSL, these are usually 1000:1000
UID=1000
GID=1000

# JWT Secret Key for Devise JWT Authentication
# Generate a new secret key by running: docker compose exec web rails secret
# IMPORTANT: Never commit the actual .env file with real secrets!
DEVISE_JWT_SECRET_KEY=your_secret_key_here
